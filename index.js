module.exports = {
  // 関数パラメータを必ず `()` で囲う
  arrowParens: 'always',
  // オブジェクトリテタルの端にスペースを入れる
  bracketSpacing: true,
  // ASI 障害対策のため、ステートメントの最後にセミコロンを付ける
  semi: true,
  // 文字列をダブルクォートで囲う
  singleQuote: false,
  // インデントを `2` 空けるようにする
  tabWidth: 2,
  // インデントをスペースで付与する
  useTabs: false,
  // 120 文字で改行をする
  printWidth: 120
};
